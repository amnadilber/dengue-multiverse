numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0
requests>=2.31
pytest>=7.4

# Optional. Needed only by the PINN comparison in steps 06, 07, 08, 10 and 12,
# which are earlier exploratory work that the paper does not use. Everything the
# paper reports runs without it.
# tensorflow-cpu>=2.15
